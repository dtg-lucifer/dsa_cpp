{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile and run",
      "type": "shell",
      "command": "g++ -std=c++17 -o ./.bin/out main.cpp ./problems/*.cpp && ./.bin/out < input > output",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
